---
title: "Rate Limits Endpoint"
description: "Check your current API key rate limit status and remaining quota."
---

## `GET` `/webcast/rate_limits`

Check your current API key rate limit status and remaining quota.

### Authentication

Pass your API key as a query parameter:

```
/webcast/rate_limits?apiKey=YOUR_API_KEY
```

### Example

<CodeGroup>
```javascript Node.js
const res = await fetch('https://api.tik.tools/webcast/rate_limits?apiKey=YOUR_KEY');
const data = await res.json();
console.log(data);
```

```python Python
import requests
res = requests.get('https://api.tik.tools/webcast/rate_limits',
    params={'apiKey': 'YOUR_KEY'})
print(res.json())
```

```bash cURL
curl \"https://api.tik.tools/webcast/rate_limits?apiKey=YOUR_KEY\"
```
</CodeGroup>

<Note>
See the [REST API Overview](/rest-api/overview) for response format and error handling.
</Note>
